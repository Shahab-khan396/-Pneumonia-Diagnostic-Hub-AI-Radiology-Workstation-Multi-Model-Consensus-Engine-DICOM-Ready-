# ─── FastAPI Backend URL (Render or Local) ──────────────────────────────────
# Server-side only: never exposed to browser client bundle
FASTAPI_URL=http://127.0.0.1:8000

# ─── Internal API Key (matches INTERNAL_API_KEY set on Render) ───────────────
INTERNAL_API_KEY=
